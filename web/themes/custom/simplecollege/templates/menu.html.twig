<ul{{ attributes.addClass('flex flex-col-reverse gap-1') }}>
  {% for item in items %}
    <li{{ item.attributes }}>
      <a href="{{ item.url }}" class="inline-block px-3 py-1 rounded no-underline">
        {{ item.title }}
      </a>
    </li>
  {% endfor %}
</ul>
